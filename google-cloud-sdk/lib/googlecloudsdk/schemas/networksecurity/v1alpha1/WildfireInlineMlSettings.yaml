$schema: "http://json-schema.org/draft-06/schema#"

title: networksecurity v1alpha1 WildfireInlineMlSettings export schema
description: A gcloud export/import command YAML validation schema.
type: object
required:
- inlineMlConfigs
additionalProperties: false
properties:
  COMMENT:
    type: object
    description: User specified info ignored by gcloud import.
    additionalProperties: false
    properties:
      template-id:
        type: string
      region:
        type: string
      description:
        type: string
      date:
        type: string
      version:
        type: string
  UNKNOWN:
    type: array
    description: Unknown API fields that cannot be imported.
    items:
      type: string
  fileExceptions:
    description: List of files to exclude from WildFire Inline ML analysis.
    type: array
    items:
      $ref: WildfireInlineMlFileException.yaml
  inlineMlConfigs:
    description: |-
      List of Inline ML configs to enable in WildFire Inline ML analysis.
    type: array
    items:
      type: string
      enum:
      - ELF
      - INLINE_ML_CONFIG_UNSPECIFIED
      - MACHO
      - MS_OFFICE
      - OOXML
      - POWERSHELL_SCRIPT1
      - POWERSHELL_SCRIPT2
      - SHELL
      - WINDOWS_EXECUTABLE
