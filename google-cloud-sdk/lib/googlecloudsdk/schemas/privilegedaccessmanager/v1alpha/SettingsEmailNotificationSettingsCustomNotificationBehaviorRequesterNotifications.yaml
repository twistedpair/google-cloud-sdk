$schema: "http://json-schema.org/draft-06/schema#"

title: privilegedaccessmanager v1alpha SettingsEmailNotificationSettingsCustomNotificationBehaviorRequesterNotifications
  export schema
description: A gcloud export/import command YAML validation schema.
type: object
additionalProperties: false
properties:
  COMMENT:
    type: object
    description: User specified info ignored by gcloud import.
    additionalProperties: false
    properties:
      template-id:
        type: string
      region:
        type: string
      description:
        type: string
      date:
        type: string
      version:
        type: string
  UNKNOWN:
    type: array
    description: Unknown API fields that cannot be imported.
    items:
      type: string
  entitlementAssigned:
    description: Notification mode for entitlement assigned.
    type: string
    enum:
    - DISABLED
    - ENABLED
    - NOTIFICATION_MODE_UNSPECIFIED
  grantActivated:
    description: Notification mode for grant activated.
    type: string
    enum:
    - DISABLED
    - ENABLED
    - NOTIFICATION_MODE_UNSPECIFIED
  grantActivationFailed:
    description: Notification mode for grant activation failed.
    type: string
    enum:
    - DISABLED
    - ENABLED
    - NOTIFICATION_MODE_UNSPECIFIED
  grantDenied:
    description: Notification mode for grant denied.
    type: string
    enum:
    - DISABLED
    - ENABLED
    - NOTIFICATION_MODE_UNSPECIFIED
  grantEnded:
    description: Notification mode for grant ended.
    type: string
    enum:
    - DISABLED
    - ENABLED
    - NOTIFICATION_MODE_UNSPECIFIED
  grantExpired:
    description: Notification mode for grant request expired.
    type: string
    enum:
    - DISABLED
    - ENABLED
    - NOTIFICATION_MODE_UNSPECIFIED
  grantExternallyModified:
    description: Notification mode for grant externally modified.
    type: string
    enum:
    - DISABLED
    - ENABLED
    - NOTIFICATION_MODE_UNSPECIFIED
  grantRevoked:
    description: Notification mode for grant revoked.
    type: string
    enum:
    - DISABLED
    - ENABLED
    - NOTIFICATION_MODE_UNSPECIFIED
